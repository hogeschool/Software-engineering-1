Functional programming practicum:

[1 point] 1) define a function "collapse" that takes as input a pair of integers, and returns a triple with elements:
 - the sum
 - the difference
 - the product
of the two elements of the input.

[2 points] 2) define a recursive function "eval" that evaluates a boolean expression tree made up of boolean values, "or" of boolean expressions, "and" of boolean expressions.

[2 points] 3) define a record "person" with a name, a surname, and an age. Define a "print" function that returns a string describing the person.
